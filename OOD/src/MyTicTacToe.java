public class MyTicTacToe {
    /*
    LC348

    BEHAVIOR
    Player:
    1. startGame
    2. placePieces
    GameService:
    1.checkWin

    ABSTRACT
    User: id, is_first_hand, (B/W); Board
    Board: id, List<Player>, Pieces;
    Pieces: Type, coordinate;
    enum Type{ BLACK, WHITE;}
    GameService: List<Player>, List<Board>, startGame, checkWin, play

    if the pieces is chess, then
    1. Abstract method boolean canPlace()
    2. Pieces
     */

}
